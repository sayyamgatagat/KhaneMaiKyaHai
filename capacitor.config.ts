import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'khane-mai-kya-hai',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
